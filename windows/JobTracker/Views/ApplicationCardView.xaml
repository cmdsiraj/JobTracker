<UserControl x:Class="JobTracker.Views.ApplicationCardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{StaticResource CardBackgroundBrush}" CornerRadius="10"
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="12" Margin="0,0,0,10"
            Cursor="Hand"
            MouseLeftButtonDown="Card_MouseLeftButtonDown"
            MouseMove="Card_MouseMove"
            MouseLeftButtonUp="Card_MouseLeftButtonUp">
        <Border.Effect>
            <DropShadowEffect BlurRadius="6" ShadowDepth="1" Opacity="0.08" Color="Black"/>
        </Border.Effect>
        <StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="CompanyText" Grid.Column="0" FontWeight="SemiBold" FontSize="13" TextTrimming="CharacterEllipsis"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock x:Name="OutreachBadge" Text="↗" Foreground="{StaticResource WarningBrush}" FontWeight="Bold" Margin="4,0,0,0" Visibility="Collapsed"/>
                    <TextBlock x:Name="ReviewBadge" Text="⚠" Margin="4,0,0,0" Visibility="Collapsed"/>
                </StackPanel>
            </Grid>
            <TextBlock x:Name="RoleText" Foreground="{StaticResource SecondaryTextBrush}" FontSize="12" TextWrapping="Wrap" Margin="0,2,0,0"/>
            <TextBlock x:Name="LocationText" Foreground="{StaticResource SecondaryTextBrush}" FontSize="10" Margin="0,2,0,0"/>
            <Grid Margin="0,6,0,0">
                <StackPanel Orientation="Horizontal">
                    <Border x:Name="CycleBadge" Background="#26357ABD" CornerRadius="8" Padding="6,1" Visibility="Collapsed">
                        <TextBlock x:Name="CycleText" FontSize="10"/>
                    </Border>
                    <Border x:Name="SourceBadge" Background="{StaticResource BorderBrush}" CornerRadius="8" Padding="6,1" Margin="4,0,0,0" Visibility="Collapsed">
                        <TextBlock x:Name="SourceText" FontSize="10"/>
                    </Border>
                </StackPanel>
                <TextBlock x:Name="UpdatedText" FontSize="10" Foreground="{StaticResource TertiaryTextBrush}" HorizontalAlignment="Right"/>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>
