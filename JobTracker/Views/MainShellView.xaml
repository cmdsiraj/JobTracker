<UserControl x:Class="JobTracker.Views.MainShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="1100">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="{StaticResource SidebarBackgroundBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ListBox x:Name="SidebarList" BorderThickness="0" Background="Transparent"
                         Margin="0,8" SelectionChanged="SidebarList_SelectionChanged">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="{x:Type ListBoxItem}">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="Focusable" Value="False"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </ScrollViewer>
        </Border>

        <!-- Right side -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="12,10">
                <DockPanel>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                        <ProgressBar x:Name="SyncProgress" IsIndeterminate="True" Width="16" Height="16"
                                     Visibility="Collapsed" Margin="0,0,8,0"/>
                        <TextBlock x:Name="SyncStatusText" VerticalAlignment="Center" FontSize="11"
                                   Foreground="{StaticResource SecondaryTextBrush}" Margin="0,0,10,0" Visibility="Collapsed"/>
                        <Button x:Name="SyncNowButton" Content="🔄 Sync Now" Click="SyncNow_Click" Padding="10,6"/>
                        <Button Content="⚙" FontSize="14" Padding="8,6" Margin="8,0,0,0" Click="Settings_Click" ToolTip="Settings"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                        <Button Content="➕ Add Manually" Padding="10,6" Click="AddManually_Click"/>
                        <Button x:Name="FilterButton" Padding="10,6" Margin="8,0,0,0" Click="FilterButton_Click"/>
                        <Button Content="📄 Activity Log" Padding="10,6" Margin="8,0,0,0" Click="ActivityLog_Click"/>
                        <Button x:Name="ImportButton" Content="⬇️ Import Mail Archive…" Padding="10,6" Margin="8,0,0,0" Click="Import_Click"/>
                    </StackPanel>
                    <TextBox x:Name="SearchBox" VerticalAlignment="Center" Margin="16,0"
                             TextChanged="SearchBox_TextChanged" ToolTip="Search company, role, tags…"/>
                </DockPanel>
            </Border>

            <Grid Grid.Row="1">
                <ContentControl x:Name="DetailHost"/>
                <Border x:Name="OverlayHost" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                        Margin="20" Visibility="Collapsed"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
