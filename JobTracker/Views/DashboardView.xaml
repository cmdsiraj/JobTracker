<UserControl x:Class="JobTracker.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:JobTracker.Controls">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <controls:TimelineScrubber x:Name="Scrubber" Margin="0,0,0,10"/>
            <ScrollViewer x:Name="CycleChipsScroll" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Margin="0,0,0,16">
                <StackPanel x:Name="CycleChipsPanel" Orientation="Horizontal"/>
            </ScrollViewer>
            <StackPanel x:Name="EmptyState" HorizontalAlignment="Center" Margin="0,60,0,0" Visibility="Collapsed">
                <TextBlock Text="📊" FontSize="40" HorizontalAlignment="Center"/>
                <TextBlock Text="No Data in This Scope" FontSize="16" FontWeight="SemiBold" Margin="0,8,0,4" HorizontalAlignment="Center"/>
                <TextBlock Text="Adjust the time range or cycle filters, run a sync, or import a mail archive."
                           Foreground="{StaticResource SecondaryTextBrush}" HorizontalAlignment="Center"/>
            </StackPanel>
            <StackPanel x:Name="ContentPanel"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
