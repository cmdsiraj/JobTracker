<Application x:Class="JobTracker.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:JobTracker.Converters">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Palette -->
            <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#FFF7F7F8"/>
            <SolidColorBrush x:Key="CardBackgroundBrush" Color="#FFFFFFFF"/>
            <SolidColorBrush x:Key="SidebarBackgroundBrush" Color="#FFEFEFF2"/>
            <SolidColorBrush x:Key="BorderBrush" Color="#FFE2E2E6"/>
            <SolidColorBrush x:Key="AccentBrush" Color="#FF357ABD"/>
            <SolidColorBrush x:Key="PrimaryTextBrush" Color="#FF1B1B1F"/>
            <SolidColorBrush x:Key="SecondaryTextBrush" Color="#FF6B6B72"/>
            <SolidColorBrush x:Key="TertiaryTextBrush" Color="#FFA0A0A6"/>
            <SolidColorBrush x:Key="DangerBrush" Color="#FFDC3545"/>
            <SolidColorBrush x:Key="SuccessBrush" Color="#FF22A55D"/>
            <SolidColorBrush x:Key="WarningBrush" Color="#FFF5A623"/>

            <!-- Converters -->
            <conv:StatusToBrushConverter x:Key="StatusToBrush"/>
            <conv:StatusToGlyphConverter x:Key="StatusToGlyph"/>
            <conv:StatusToDisplayNameConverter x:Key="StatusToDisplayName"/>
            <conv:InverseBooleanConverter x:Key="InverseBoolean"/>
            <conv:NullToVisibilityConverter x:Key="NullToVisibility"/>
            <conv:EmptyStringToVisibilityConverter x:Key="EmptyStringToVisibility"/>
            <conv:CountToVisibilityConverter x:Key="CountToVisibility"/>
            <conv:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
            <conv:RelativeTimeConverter x:Key="RelativeTime"/>
            <conv:PercentConverter x:Key="Percent"/>
            <conv:EnumEqualsConverter x:Key="EnumEquals"/>
            <BooleanToVisibilityConverter x:Key="NativeBoolToVisibility"/>

            <!-- Base styles -->
            <Style TargetType="{x:Type Window}">
                <Setter Property="Background" Value="{StaticResource WindowBackgroundBrush}"/>
                <Setter Property="FontFamily" Value="Segoe UI"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="TextElement.Foreground" Value="{StaticResource PrimaryTextBrush}"/>
            </Style>

            <Style TargetType="{x:Type TextBox}">
                <Setter Property="Padding" Value="8,6"/>
                <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
            </Style>

            <Style TargetType="{x:Type PasswordBox}">
                <Setter Property="Padding" Value="8,6"/>
                <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
            </Style>

            <Style TargetType="{x:Type ComboBox}">
                <Setter Property="Padding" Value="8,6"/>
            </Style>

            <Style x:Key="PrimaryButton" TargetType="{x:Type Button}">
                <Setter Property="Padding" Value="16,8"/>
                <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Opacity" Value="0.5"/>
                    </Trigger>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Opacity" Value="0.9"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="PlainButton" TargetType="{x:Type Button}">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Padding" Value="4"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Opacity" Value="0.7"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="CardBorder" TargetType="{x:Type Border}">
                <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
                <Setter Property="CornerRadius" Value="12"/>
                <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="16"/>
            </Style>

            <Style x:Key="SectionHeader" TargetType="{x:Type TextBlock}">
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Margin" Value="0,0,0,8"/>
            </Style>

            <Style x:Key="CaptionText" TargetType="{x:Type TextBlock}">
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
